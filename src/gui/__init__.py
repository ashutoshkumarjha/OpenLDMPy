"""Qt GUI for OpenLDM.

Kept separate from the ``LULC`` processing package (processing
modules must not import Qt). Everything in here is free to import PyQt5;
nothing in ``LULC`` imports anything from here.
"""
