# Core Data Science
numpy>=1.24.0
pandas>=2.0.0
scipy>=1.10.0

# Geospatial Processing
rasterio>=1.4.0
# GDAL is optional but often required by rasterio.
# It's best installed via conda, but we list the python bindings here.
GDAL>=3.4.0
geopandas>=1.0.0
shapely>=2.0.0
matplotlib>=3.8.0  # >=3.8 required for NumPy 2 compatibility; cartography.py (legend/scale bar/north arrow)

# Machine Learning
scikit-learn>=1.7.0
joblib>=1.3.0
statsmodels>=0.14.0  # Wald-test p-values/significance stars for logistic coefficients

# GUI (Existing)
# Install these via conda/environment.yml, not pip: a plain
# `pip install -r requirements.txt` re-fetches these from PyPI and silently
# overwrites conda's matched pyqt/pyqtwebengine/qt-webengine build on disk
# (conda's package DB keeps no record of the clobber), which can reintroduce
# a QtWebEngine crash on the Help tab. Left commented rather than removed so
# `pip install -r requirements.txt` in a pip-only (non-conda) setup still
# works.
# PyQt5>=5.15.0
# PyQtWebEngine>=5.15.0

# Scenario files (GUI Open/Save, main.py --config)
PyYAML>=6.0

# Testing
pytest>=8.0.0
